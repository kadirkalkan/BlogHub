@model EditArticleViewModel
@{
    ViewData["Title"] = "EditArticle";
}

<h1>Edit Article</h1>

<form asp-controller="Data" asp-action="EditArticle" enctype="multipart/form-data">
    <input type="hidden" asp-for="Id" />
    <input type="hidden" asp-for="ArticlePictureName" />
    <div class="row">
        <div class="col-md-12">
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8">
            <div class="form-group">
                <label asp-for="Content" class="control-label"></label>
                <br />
                <textarea asp-for="Content" style="width:100%"></textarea>
                <span asp-validation-for="Content" class="text-danger"></span>
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">
                <label asp-for="ArticlePicture" class="control-label"></label>
                <div class="custom-file">
                    <input asp-for="ArticlePicture" class="custom-file-input" id="customFile">
                    <label id="custom-file-label" class="custom-file-label" for="customFile"></label>
                </div>
                <span asp-validation-for="ArticlePicture" class="text-danger"></span>
            </div>
        </div>
    </div>
    <div class="form-group">
        <input type="submit" value="submit" class="btn btn-sm btn-primary rounded-0 text-uppercase" />
        <a asp-controller="Home" asp-action="Index" class="btn btn-sm btn-primary rounded-0 text-uppercase"><i class="fas fa-fast-backward"></i> Back to List</a>
    </div>
</form>


@section scripts{
    <script type="text/javascript">
        console.log('@Model.ArticlePictureName')
        $("#custom-file-label").html('@Model.ArticlePictureName');

        // Add the following code if you want the name of the file appear on select
        $(".custom-file-input").on("change", function () {
            var fileName = $(this).val().split("\\").pop();
            $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
        });


        


    </script>
}

